name: Cron-based Workflow
run-name: cron-based-workflow

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  scheduled-job:
    runs-on: ubuntu-latest
    steps:
      - name: Run scheduled task
        run: echo "This job runs every 2 minutes"
